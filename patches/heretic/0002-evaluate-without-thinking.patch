diff --git a/src/heretic/config.py b/src/heretic/config.py
index eef2eb3..b7d99b9 100644
--- a/src/heretic/config.py
+++ b/src/heretic/config.py
@@ -238,6 +238,16 @@ class Settings(BaseSettings):
         description="Maximum number of tokens to generate for each response.",
     )
 
+    disable_thinking: bool = Field(
+        default=False,
+        description=(
+            "Whether to pass enable_thinking=False to the chat template when generating "
+            "responses for direction extraction and evaluation. Thinking models otherwise "
+            "start their response inside a reasoning block, so scoring sees the opening of "
+            "the chain of thought instead of the answer. Templates without the flag ignore it."
+        ),
+    )
+
     response_prefix: str | None = Field(
         default=None,
         description=(
diff --git a/src/heretic/model.py b/src/heretic/model.py
index 9af26b3..3821a84 100644
--- a/src/heretic/model.py
+++ b/src/heretic/model.py
@@ -618,6 +618,16 @@ class Model:
                     weight_A.data = lora_A.to(weight_A.dtype)
                     weight_B.data = lora_B.to(weight_B.dtype)
 
+    @property
+    def chat_template_kwargs(self) -> dict[str, Any]:
+        # A thinking model's generation prompt ends inside a reasoning block, so
+        # both the refusal directions and the scores would be derived from the
+        # opening of the chain of thought rather than from the answer. Chat
+        # templates that don't know the flag simply ignore it.
+        if not self.settings.disable_thinking:
+            return {}
+        return {"enable_thinking": False}
+
     def generate(
         self,
         prompts: list[Prompt],
@@ -639,6 +649,7 @@ class Model:
                 chats,
                 add_generation_prompt=True,
                 tokenize=False,
+                **self.chat_template_kwargs,
             ),
         )
 
